# Extracted content batches

The curated corpus (extracted, reviewed, fixture-gated) is developed in a
private content repository and ships separately. Any `*.jsonl` emission-envelope
files placed here load through `chisel_templates()` — the seam is public, the
content is not.
